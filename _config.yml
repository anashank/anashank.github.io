title: Anand Shankar
description: Personal website and portfolio

# Use a GitHub Pages supported theme to avoid build problems
theme: jekyll-theme-primer

author:
  name: "Anand Shankar"

# Keep URL blank for local development; GitHub Pages will set the live URL
url: ""

# Optional: enable a projects collection
collections:
  projects:
    output: true

exclude:
  - Gemfile
  - Gemfile.lock
